apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitdb.fullname" . }}-test-connection"
  labels:
    {{- include "gitdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitdb.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
